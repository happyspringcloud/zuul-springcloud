server:
  port: ${server_port:8080}
 
spring:
  cloud:
    config:
      uri: ${config_servers:http://localhost:9001}
      searchPaths: zuul-ddd
---
spring:
  application:
    name: zuul-ddd
  profiles:
    active: ${profile:local}
    include: common